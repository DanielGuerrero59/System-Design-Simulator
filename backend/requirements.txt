# Loose lower bounds for now; we'll pin exact versions with `pip freeze`
# once the app runs, so a stranger cloning this gets a reproducible install.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9

# NumPy is intentionally absent: the M/M/1 math is scalar arithmetic and
# imports nothing from it. Add it back when traffic-ramp presets need
# vectorised timesteps, rather than shipping 34 MB unused.

# Dev / test
pytest>=8.0
httpx>=0.27
