# Loose lower bounds for now; we'll pin exact versions with `pip freeze`
# once the app runs, so a stranger cloning this gets a reproducible install.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
numpy>=2.0

# Dev / test
pytest>=8.0
httpx>=0.27
